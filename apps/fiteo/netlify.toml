[build]
  command = "cd ../.. && pnpm --filter fiteo build"
  publish = "apps/fiteo/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes serve index.html
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
